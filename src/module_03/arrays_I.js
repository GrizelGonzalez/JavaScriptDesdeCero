/*buscar la posición del número en el Array*/

let numeros=[1,2,3,4,5,6];
 
console.log(numeros[1]);/*aquí estamos buscando la posicón del número 1
que se encuentra en el arreglo*/

/*sumar dos números del arreglo*/

console.log(numeros[0]+numeros[1]);

/*Arrays con strings*/
let palabras=['Miriam', 'Grizel', 'Gonzalez'];

console.log(palabras[2]); //buscar la posicion del string
console.log(palabras[2].length); //buscar la cantidad de letras que contiene el string
console.log(`La palabra ${palabras[2]} tiene ${palabras[2].length} letras`);
